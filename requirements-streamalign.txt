# Stream Alignment Engine + harvester dependencies. `make venv` installs this file together
# with requirements.txt into the one virtualenv, `.venv`; run every tool with .venv/bin/python.
#
# Core engine (P0-P4: groundtruth/audio/align/skips/graph/solve) needs only numpy
# plus a system ffmpeg on PATH. The original-track <-> mix feature alignment (track_mix,
# chroma + DTW) and the harvester need librosa, which pulls numba/llvmlite -- the reason
# `make venv` prefers python3.13 (numba's wheels lag new Pythons; the 3.13 build is the
# tested one).
numpy>=2
librosa>=0.11
soundfile>=0.13     # harvester excerpts + the split/excerpt tests write real FLAC/WAV
pyflakes>=3         # the undefined-name test guard (fails, never skips, by design)
