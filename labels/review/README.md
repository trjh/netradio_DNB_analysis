# labels/review/ — the align tool's semi-manual annotations

`<stem>.origNNN.review.tsv` — machine context rows plus hand notations made in the
align review surfaces (`/align/review` and the Mac review app in the companion
player project). **Never regenerated**: these carry hand judgement, which is why
they are in git. Written by the player's review-note route and auto-committed to
the `align-review-data` branch by its `review_sync`; they reach the authoritative
`labels/*.labels.tsv` only through the manual fold.
